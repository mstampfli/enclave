[package]
name = "enclave-media"
description = "Capture, encode, and decode audio/video. Runs the real-time hot path."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
audiopus = "0.2.0"
enclave-protocol = { path = "../protocol" }
thiserror.workspace = true
# Phase 3 adds: cpal (capture/playback), opus (audio codec).
# Phase 5 adds: nokhwa (camera), a video codec (VP9/AV1), scrap (screen).
